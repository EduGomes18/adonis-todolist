@layout('main')
@section('content')
<h1>{{ title }}</h1>
@if(old('notification'))
<div class="alert alert-primary" role="alert">
  {{old('notification')}}
</div>
@endif
<div class="list-group">
  @each(item in tasks)
  <button type="button" class="list-group-item list-group-item-action">{{item.title}}</button>
  @endeach
</div>
@endsection